#include <stdio.h>

int main() {
    int n, i;
    printf("Enter the number of times to print NIRAL: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        printf("NIRAL\n");
    }

    return 0;
}
